// Definición de la paleta corporativa y extensiones para el CDN
tailwind.config = {
    theme: {
        extend: {
            colors: {
                brand: '#F79131',   // Naranja corporativo
                dark: '#7A2C8E',    // Morado principal
                accent: '#4DB748',  // Verde de realce
            },
            fontFamily: {
                sans: ['Inter', 'sans-serif'],
            }
        }
    }
}
